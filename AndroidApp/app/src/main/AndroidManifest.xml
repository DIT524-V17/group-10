<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="martincukal.led">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />

    import com.example.android.courtcounter.R;

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dit524"
        android:label="GROUP10"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DeviceList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        android:name=".ledControl"
        android:label="LED Control"
        <activity android:name=".ledControl" />
        <activity android:name=".VideoViewCam"
            android:screenOrientation="landscape"
        />

    </application>

</manifest>